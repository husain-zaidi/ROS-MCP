## Packages
@xyflow/react | Interactive node graph visualization for ROS nodes
recharts | Charts for system metrics and message rates
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
Backend provides REST endpoints for ROS data (/api/ros/*)
ROS Bridge connection is managed by the backend, frontend polls or fetches on demand
Dark mode is the default for the robotics control interface
